name: mobile_remote
description: "Pluely mobile remote control app."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  mobile_scanner: ^7.1.3
  web_socket_channel: ^3.0.3
  provider: ^6.1.2
  path_provider: ^2.1.5
  share_plus: ^10.1.4
  image_gallery_saver_plus: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
